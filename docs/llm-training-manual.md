# LLM training manual

Markdown source for **`docs/llm-training-manual.docx`**. Replace or extend this file with your governance, dataset, and fine-tuning workflow.

Regenerate Word:

`npm --workspace backend run docx:llm-training-manual`

Steering behavior in the live product is configured in the **AI training** UI and `backend/data/ai-training-pack.json` (see `backend/src/engine/layered/aiTrainingPack.ts`).
